<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Màu nền (tuỳ chọn) -->
    <stroke
        android:width="1dp"
        android:color="#000000" /> <!-- Màu viền đen -->
    <corners android:radius="8dp" /> <!-- Bo góc (tuỳ chọn) -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" /> <!-- Khoảng cách bên trong viền -->
</shape>
